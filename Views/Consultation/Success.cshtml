@{
    ViewData["Title"] = "Успішна реєстрація";
}

<div class="container">
    <h2>Реєстрація пройшла успішно!</h2>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">
            @TempData["SuccessMessage"]
        </div>
    }

    <p>Найближчим часом з вами зв'яжуться для підтвердження консультації.</p>
</div>